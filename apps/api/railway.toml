[build]
builder = "nixpacks"
buildCommand = "pip install -r apps/api/requirements.txt"

[deploy]
startCommand = "python -m alembic -c apps/api/alembic.ini upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
